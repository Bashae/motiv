import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class StreamService {

  constructor() { }

  getPosts(type){
    // let t = type || "All";

    // return getPostsCall(t);
  }
}
